apiVersion: v1
kind: Service
metadata:
  name: shopkube-backend-service
  namespace: shopkube
spec:
  selector:
    app: shopkube-backend
  ports:
    - name: http
      port: 80
      targetPort: 80
